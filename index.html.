<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Task Manager</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      text-align: center;
      padding: 50px;
    }
    h1 {
      color: #333;
    }
    ul {
      list-style: none;
      padding: 0;
    }
    li {
      background: #fff;
      margin: 5px 0;
      padding: 10px;
      border: 1px solid #ccc;
    }
    button {
      padding: 10px 20px;
      margin-top: 20px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <h1>Task Manager</h1>
  <p>Добро пожаловать в мой проект!</p>

  <ul id="tasks">
    <li>Пример задачи: изучить HTML</li>
  </ul>

  <button onclick="addTask()">Добавить задачу</button>

  <script>
    function addTask() {
      const taskList = document.getElementById('tasks');
      const newTask = document.createElement('li');
      newTask.textContent = 'Новая задача';
      taskList.appendChild(newTask);
    }
  </script>
</body>
</html>
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Task Manager</title>
  <style>
    body { font-family: Arial; background: #f4f4f4; text-align: center; }
    #tasks { list-style: none; padding: 0; }
    li { background: #fff; margin: 5px; padding: 10px; border: 1px solid #ccc; }
  </style>
</head>
<body>
  <h1>Task Manager</h1>
  <p>Добро пожаловать!</p>
  <ul id="tasks"></ul>
  <button onclick="addTask()">Добавить задачу</button>

  <script>
    function addTask() {
      const list = document.getElementById('tasks');
      const item = document.createElement('li');
      item.textContent = 'Новая задача';
      list.appendChild(item);
    }
  </script>
</body>
</html>



